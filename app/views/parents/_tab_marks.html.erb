<script type="text/javascript">
$(document).ready( function() {
    $.ajax({
              type: 'POST',
              url: "students/" + "<%= @student.id %>" + "/subjects/" + "<%= tab_marks.subject %>", 
              contentType: "text/javascript",
          }).done(function(response){
            });
});
</script>
<div class="tab-pane fade show" id="<%= tab_marks.subject %>" role="tabpanel" aria-labelledby="<%= tab_marks.subject %>-tab">
  <table id="marks" data-sortable="true" class="table table-striped table-sm">
      <thead>
      <tr>
        <th>Teacher</th>
        <th>Mark</th>
        <th>Subject</th>
        <th>Student</th>
        <th>Mark date</th>
        <th>Notes</th>
        <th></th>
      </tr>
      </thead>
      <tbody id="marks-body-<%= tab_marks.subject %>">
      </tbody>
  </table>
</div>
<script type="text/javascript">
$(document).ready( function() {
    $.ajax({
              type: 'POST',
              url: "/parents/<%=@parent.id%>/students/" + "<%= @student.id %>" + "/subjects/" + "<%= tab_marks.subject %>", 
              contentType: "text/javascript",
          }).done(function(response){
            });
});
</script>
<div class="tab-pane fade" id="<%= tab_marks.subject %>" role="tabpanel" aria-labelledby="<%= tab_marks.subject %>-tab">
<div style="overflow: auto">
  <table id="marks" data-sortable="true" class="table table-striped table-sm">
      <thead>
      <tr>
        <th scope="col">Teacher</th>
        <th scope="col">Mark</th>
        <th scope="col">Student</th>
        <th scope="col">Mark date</th>
        <th scope="col">Notes</th>
      </tr>
      </thead>
      <tbody id="marks-body-<%= tab_marks.subject %>">
      </tbody>
  </table>
</div>
  <br>
  <p style="text-align: left" id="avg-<%= tab_marks.subject %>"></p>
</div>
<script type="text/javascript">
$(document).ready( function() {
    $.ajax({
              type: 'POST',
              url: "/parents/<%=@parent.id%>/notes/" + "<%= @student.id %>" + "/subjects/" + "<%= tab_notes.subject %>", 
              contentType: "text/javascript",
          }).done(function(response){
            });
});
</script>
<div class="tab-pane fade" id="<%= tab_notes.subject %>" role="tabpanel" aria-labelledby="<%= tab_notes.subject %>-tab">
<div style="overflow: auto">
  <table id="notes" data-sortable="true" class="table table-striped table-sm">
      <thead>
      <tr>
        <th>Note</th>
        <th>Teacher</th>
        <th>Subject</th>
        <th>Student</th>
        <th>Date</th>
      </tr>
      </thead>
      <tbody id="notes-body-<%= tab_notes.subject %>">
      </tbody>
  </table>
</div>
<script type="text/javascript">
    $(document).ready( function() {
        $.ajax({
            type: 'POST',
            url: "/parents/<%=@parent.id%>/assignments/" + "<%= @student.id %>" + "/subjects/" + "<%= tab_assignments.subject %>",
            contentType: "text/javascript",
        }).done(function(response){
        });
    });
</script>
<div class="tab-pane fade" id="<%= tab_assignments.subject %>" role="tabpanel" aria-labelledby="<%= tab_assignments.subject %>-tab">
  <table id="assigments" data-sortable="true" class="table table-striped table-sm" style="table-layout: fixed;">
    <thead>
    <tr>
      <th>Name</th>
      <th>Expiry Date</th>
      <th>Subject</th>
      <th>School Class</th>
      <th>Teacher</th>
      <th>Lecture</th>
    </tr>
    </thead>
    <tbody id="assignments-body-<%= tab_assignments.subject %>">
    </tbody>
  </table>
  <br>
  <p style="text-align: left" id="avg-<%= tab_assignments.subject %>"></p>
</div>
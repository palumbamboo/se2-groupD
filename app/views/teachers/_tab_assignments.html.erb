<script type="text/javascript">
    $(document).ready( function() {
        $.ajax({
            type: 'POST',
            url: "/teachers/<%=@teacher.id%>/assignments/" + "<%= @school_class.id %>" + "/subjects/" + "<%= tab_assignments.subject %>",
            contentType: "text/javascript",
        }).done(function(response){
        });
    });
</script>
<div class="tab-pane fade" id="<%= tab_assignments.subject %>" role="tabpanel" aria-labelledby="<%= tab_assignments.subject %>-tab">
<div style="overflow: auto">
  <table id="assignments" data-sortable="true" class="table table-striped table-sm">
    <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Expiry Date</th>
      <th scope="col">Subject</th>
      <th scope="col">Lecture</th>
      <th scope="col">Details</th>
      <th scope="col"></th>
      <th scope="col"></th>
    </tr>
    </thead>
    <tbody id="assignments-body-<%= tab_assignments.subject %>">
    </tbody>
  </table>
  </div>
  <br>
  <p style="text-align: left" id="avg-<%= tab_assignments.subject %>"></p>
</div>

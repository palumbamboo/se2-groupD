<script type="text/javascript">
    $(document).ready( function() {
        $.ajax({
            type: 'POST',
            url: "/teachers/<%=@teacher.id%>/materials/" + "<%= @school_class.id %>" + "/subjects/" + "<%= tab_materials.subject %>",
            contentType: "text/javascript",
        }).done(function(response){
        });
    });
</script>
<div class="tab-pane fade" id="<%= tab_materials.subject %>" role="tabpanel" aria-labelledby="<%= tab_materials.subject %>-tab">
  <div style="overflow: auto">
    <table id="materials" data-sortable="true" class="table table-striped table-sm">
      <thead>
      <tr>
        <th>Title</th>
        <th>Subject</th>
        <th>Creation date</th>
        <th></th>
        <th></th>
      </tr>
      </thead>
      <tbody id="materials-body-<%= tab_materials.subject %>">
      </tbody>
    </table>
  </div>
  <br>
  <p style="text-align: left" id="avg-<%= tab_materials.subject %>"></p>
</div>
